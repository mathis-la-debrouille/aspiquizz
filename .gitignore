# dependencies
node_modules/

# next.js
.next/
out/

# production
build/
dist/

# local db
*.db
*.db-journal
*.db-wal
*.db-shm
local.db*

# uploads
.uploads/

# env
.env
.env.local
.env.*.local

# testing
tests/e2e/.output/
playwright-report/
test-results/
coverage/

# misc
.DS_Store
*.pem
npm-debug.log*
pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# Open Trivia DB staging dump — regenerate with scripts/fetch-opentdb.ts.
# Deliberately NOT committed: ~500 kB of raw English trivia, roughly 125 000 tokens,
# that nobody reviews and no build reads. It is the intermediate between fetching and
# translating, and leaving it in the repo means every clone — and every coding agent
# that reads the tree — pays for it. The reviewed French output IS committed.
scripts/data/opentdb-raw.json
